pymongo>=4.6.0
openai>=1.0.0
boto3>=1.34.0
langfuse>=2.0.0
# Waveform generation uses FFmpeg (via Lambda Layer) + pure Python RMS calculation
# No additional dependencies required - avoids heavy ML libs (librosa, numpy, scipy)

